name: World
on:
  workflow_run:
    workflows:
      - Hello
    types:
      - completed

jobs:
  world:
    if: contains(fromJSON(steps.labels.outputs.json), 'iOS')
    runs-on: ubuntu-latest
    steps:
      - run: echo "World"
